= render :partial => "/admin/event_types/left_menu"

%h2 Plan zajęć
%h3 Dodaj wydarzenie/zajęcia

= form_for [:admin, @event], :url => admin_timetable_events_path do |f|
  = render :partial => "event_form", :locals => { :f => f, :event => @event }
  %p= f.submit "Dodaj"