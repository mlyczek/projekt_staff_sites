- render :partial => "admin/subjects/subject_inner_menu"

%h2 Edytuj termin zajęć

- form_for [:admin, @hour.subject, @hour] do |f|
  =render :partial => "hours_form", :locals => { :f => f, :hour => @hour }
  %p
    = f.label :start, "Godziny"
    = f.text_field :start, :size => 4, :value => @hour.start.strftime("%H:%M")
    \-
    = f.text_field :end, :size => 4, :value => @hour.end.strftime("%H:%M")

  %p= f.submit "Zmień"